<form autocomplete="off" onsubmit="event.preventDefault();">
	{{#each pontos as |ponto key|}}
		<h3>{{localize (concat "TRESDETV.Pontos." key ".label")}}</h3>
		<div class="form-group">
			<label>Bônus</label>
			<input type="text" name="system.pontos.{{key}}.bonus" value="{{ponto.bonus}}" data-dtype="Number" />
		</div>
		<div class="form-group">
			<label>Multiplicator</label>
			<input type="text" name="system.pontos.{{key}}.mult" value="{{ponto.mult}}" data-dtype="Number" />
		</div>
	{{/each}}

	<button type="submit"><i class="far fa-save"></i> {{localize "Save"}}</button>
</form>